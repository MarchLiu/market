include "serialization"
akka {

  actor {
    provider = remote
  }

  remote {
    enabled-transports = ["akka.remote.netty.tcp"]
    netty.tcp {
//      hostname = "192.168.50.179"
      port = 2555
    }
  }

}